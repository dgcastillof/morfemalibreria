{
  "name": "morfemalibreria",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run build && node test/test.js",
    "lint": "eslint src/**/*.js test/**/*.js",
    "format": "prettier --write --parser html \"src/**/*.{js,css,html,ejs}\"",
    "build": "node build.js",
    "start": "npx http-server dist",
    "generate:sitemap": "node generate-sitemap.js"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "esbuild": "^0.25.8",
    "eslint": "8.57.0",
    "http-server": "^14.1.1",
    "prettier": "^3.6.2",
    "ejs": "^3.1.9"
  }
}
